一.获取明星信息案例
  1.1点击按钮后去明星信息；
 调用jQuery，调用模板template的插件
  1.2点击按钮是调用ajax；通过ajax去请求后台数据；前台设定好接受数据样式“JSON”；
 1.3控制台打印输出是 数组样式的；使用模板引擎必须是对象；转换成对象，
var  data={};的形式； 这也就建立好数据了

1.4 利用模板引擎把数据打印出来；先设置基本模板样式；type=“text/template”,id="name"
 1.5用模板引擎循环遍历对象，得到每一项，调用响应value就可以了
1.6合并模板数据  template（name，data）；
1.7最后得到所有明星列表；追加到table中，大功告成；
    错误点：对象用  “，”好分开；后台获取数据：file_get_contents（数据库内容）


二.聊天记录自动回复
  2.1点击按钮上传信息；点击按钮，把输入框的内容获取，模板引擎渲染，得到数据他把他数据话，模板引擎必须是对象；
注意：点击上传之后要把输入框中的内容清空，$(teasge).val("")值为空；
  2.2模板引擎三部曲1.准备数据  2 创建数据 3合并准备数据和创建 数据；最后追加到 message中；
  2.3点击按钮同时向后台请求数据，利用$.ajax({})的方式向后台请求；也要用到模板引擎，三部，1准备数据2.创建数据3.合并数据；
  2.4追加到message中。

三 瀑布流加载
   3.1定义一个方法 来后去下一页；通过ajax请求信息；成功申请后拼劲用模板引擎拼接数据
  然后合并；然后最大大盒子里；再用封装好的瀑布流及你选个平铺

  3.2 那个小按钮请求信息；设置样式，添加属性
   3.3进行判断；避免重复操作；判断有没
    3.4自定义属性存储数据；	// 缓存下一页的页码  自定义属性：存储数据
3,5页面请求完成，按钮样式复$('.tips').attr('data-page',info.page);
3.6 点击当前按钮请求下一张。信息条直接调用组存储的Index
4,1 或浏览器窗口高度 var wHeight=$(window).height();
4.2 页面顶部滚出的高度
4.3 items.height() items  高度
4.4var itemsTop=$('.items').offset().top; 
4.5var t=itemsHeight+itemsTop-wHeight-sTop;
  
					//console.log(t);

四 canvas中计算直角三角形的高度
   答首先知道你要得到这是干什么的要算出通过高度算出左右两边的xy轴坐标点；
       数学算法；sin 60度=h/100;  得出h=sin角度*边长；
4.0因为HTML只提供Math.sin(这里只能弧度)；
4.1圆是360度相当于弧度2*pi， 有角度就能算出弧度；Math.PI*角度/180度；就是弧度；
4.2  有了弧度，就可以通过Math.sin(弧度)
4.3 高度 h=Math.sin(弧度）*边长；（length）得到高度；
4.4 已知定点坐标了，让200,100，边长100，高度算出；算左右两边的坐标点
  4.5首先左右边的x点，他就是定点200-边长的一半100/2;右边200+边长的一半100/2；
  4.6y的上下坐标；就等于上面的y的坐标点加上三角形的高H就可以，左右两边y都相等；











