对象和布尔值较
&& 且预算符 判断左边是否为真？ 左边为真，返回右边；左边为假，返回左边；
 且运算符，只要为假，就直接返回；
|| 或运算符， 判断左边是否为真？ 左边为真，返回左边；左边为假，返回右边；或运算符有真就返回真；
2基本数值类型 或值类型   数字 字符串 布尔值 null undefined
3复杂数据类型或引用数据类型   Math Date Function Array
4.typeof返回的是小写带有字符串的内容
 typeof null 返回的时object
6 == 判断值是否相等，会隐式转换
5.数值类型和字符串类型作比较时，会把字符串转换为值类型
  布尔类型和其他类型转换时，会把优先转换布尔值为字符串；
  对象和对象比较时 比较是内存中的地址
  对象和字符串比较时，会把对象转换为字符串；


7this指向判断？
7.1判断this指向谁 首先要看this在那个函数体内部；最终由该函数的调用方式来决定this的指向
7.2  
7.3__proto__IE8版本不支持





8.能不能优化，